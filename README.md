# akuna-systems

Infrastructure validation runs automatically on pushes and pull requests. Production deployment is modeled as a manually triggered GitHub Actions job and assumes an AWS IAM role configured for GitHub OIDC.